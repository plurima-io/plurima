<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Kafka client internals are extremely chatty at DEBUG/INFO. Pin to WARN so the
         scenario PASS/FAIL output is readable. Plurima's own logs default to INFO. -->
    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="io.plurima" level="INFO"/>

    <root level="WARN">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>

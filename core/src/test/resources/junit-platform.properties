# Ensure integration tests against a shared broker run sequentially.
# Parallel test execution would cause multiple share-consumer sessions to compete
# for the same broker resources simultaneously, leading to unpredictable delivery latency.
junit.jupiter.execution.parallel.enabled=false
